import { FilterAutocompletePipe } from './filter-autocomplete.pipe';

describe('FilterAutocompletePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAutocompletePipe();
    expect(pipe).toBeTruthy();
  });
});
